import {
    WEATHER_API_BASE_URL,
    WEATHER_API_KEY,
    SITE_BASE_PATH,
} from "../config";

export const BASE_URL = WEATHER_API_BASE_URL;
export const KEY = WEATHER_API_KEY; // api key from weatherapi.com
export const BASE_PATH = SITE_BASE_PATH;
